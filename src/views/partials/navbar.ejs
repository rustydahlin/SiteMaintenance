<nav class="navbar navbar-expand-md navbar-dark bg-dark fixed-top">
  <div class="container-fluid">
    <a class="navbar-brand fw-bold" href="/">
      <i class="bi bi-tools me-1"></i><%= appName %>
    </a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarMain">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarMain">
      <ul class="navbar-nav ms-auto mb-2 mb-md-0">
        <% if (user) { %>
          <li class="nav-item">
            <span class="nav-link text-light opacity-75">
              <i class="bi bi-person-circle me-1"></i><%= user.DisplayName %>
              <% if (user.roles && user.roles.length) { %>
                <span class="badge bg-secondary ms-1"><%= user.roles[0] %></span>
              <% } %>
            </span>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/profile"><i class="bi bi-gear me-1"></i>Profile</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/auth/logout"><i class="bi bi-box-arrow-right me-1"></i>Logout</a>
          </li>
        <% } else { %>
          <li class="nav-item">
            <a class="nav-link" href="/auth/login"><i class="bi bi-box-arrow-in-right me-1"></i>Login</a>
          </li>
        <% } %>
      </ul>
    </div>
  </div>
</nav>
